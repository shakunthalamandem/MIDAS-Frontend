import React from 'react';
import { useEffect, useState } from "react";
import {
  Box,
  FormGroup,
  FormControlLabel,
  Radio,
  RadioGroup,
  CircularProgress,
} from "@mui/material";

interface DetailedGapProps {
    selectedFilters: any;
    }

const DetailedGapData: React.FC<DetailedGapProps> = ({ selectedFilters }) => {
  return (
    <div>DetailedGapData</div>
  )
}

export default DetailedGapData;