import React from "react";
import ActiveAgentCard, { ActiveAgentCardProps } from "./ActiveAgentCard";

const AIUnsupervisedMarketInsightsAgent: React.FC<ActiveAgentCardProps> = (props) => (
  <ActiveAgentCard {...props} />
);

export default AIUnsupervisedMarketInsightsAgent;
